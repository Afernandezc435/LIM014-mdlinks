/*const mdLink = require("./md-links.js");

const test = __dirname + "/test";
//constante de prueba para la lectura de archivos

const validate = proccess.argv.indexOf("--validate") > 0;
const stats = proccess.argv.indexOf("--stats") > 0;

mdLinks(test, { validate: validate })
  .then((res) => {
    res.forEach((link) => {
      console.log(link.file, link.href, link.text);
    });
  })
  .catch((err) => {
    console.log(err);
  });*/
